# write these pipeline in the azure devops pipeline UI no need to add in the code repo

trigger:
  branches:
    include:
      - main
  paths:
    include:
      - vote/**

extends:
  template: pipelines/ci-template.yml

  parameters:
    serviceName: 'vote-app'
    servicePath: 'vote'